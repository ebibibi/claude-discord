{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file=\"$TOOL_INPUT_file_path$TOOL_INPUT_filePath\"; if [[ \"$file\" == *.py ]]; then cd /home/ebi/claude-discord && uv run ruff format \"$file\" 2>/dev/null; fi",
            "timeout": 10000
          }
        ]
      }
    ]
  }
}
